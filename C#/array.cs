using System;

class array
{
    static void Main()
    {
        int[] a = { 3, 4, 5, 6, 7, 8 };

        for (int i = 0; i < a.Length; i++)
        {
            Console.WriteLine(a[i]);
        }

        Console.ReadLine();
    }
}
